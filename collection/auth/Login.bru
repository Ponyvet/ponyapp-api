meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/auth/sign-in/email
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@ponyvet.com",
    "password": "Mvn32@E5Sxm9"
  }
}

tests {
  test("Should return success with valid credentials", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
