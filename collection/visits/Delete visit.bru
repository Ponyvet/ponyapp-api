meta {
  name: Delete visit
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/visits/visit-id-example
  body: none
  auth: none
}

tests {
  test("Should return 401 without token", function() {
    expect(res.getStatus()).to.equal(401);
  });

  test("Should return 204 with valid token", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
